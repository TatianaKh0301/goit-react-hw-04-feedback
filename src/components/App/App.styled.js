import styled from "@emotion/styled";
import bgImage from "../../image/bgimage_feedback.jpg";

export const Wrapper = styled.div`
    height: 100vh;
    padding: 50px;
    color: #ffffff;
    background-image: url(${bgImage});
`; 